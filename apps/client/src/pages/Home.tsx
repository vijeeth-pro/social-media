import { Button } from "@example/ui";


export const Home = () => {
  // const { data } = trpc.user.useQuery();
  return (
    // <HomeContainer>
    <div>
      {/* {`Role: ${data?.role}`} */}
      <Button />

    </div>
    // </HomeContainer>
  );
};